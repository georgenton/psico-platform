{
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@psico/web",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
