---
"@psico/web": minor
---

implement landing page, auth pages and dashboard with Next.js 14 App Router, Tailwind v4, Server Actions and httpOnly cookies

New pages and features:

- Landing page with hero, features, pricing and CTA sections
- Auth pages: login and register with Server Actions and form validation
- Dashboard layout with sidebar navigation and user session display
- httpOnly cookie-based session management via Next.js middleware
- Tailwind v4 styling with custom design tokens
