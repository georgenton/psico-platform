-- Fase G — Evolución series switches from the global pct (no defensible
-- psychological interpretation, V2 principle 2) to data COVERAGE: how much
-- signal backs the map that month. Additive; pre-Fase-G rows stay null.
ALTER TABLE "EmotionalMapSnapshot" ADD COLUMN "coverage" DOUBLE PRECISION;
